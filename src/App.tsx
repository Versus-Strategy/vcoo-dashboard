import { BrowserRouter } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <div className="min-h-screen">
        {/* Las rutas se agregarán aquí */}
      </div>
    </BrowserRouter>
  );
}

export default App;
